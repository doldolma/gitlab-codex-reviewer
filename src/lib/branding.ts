/** Product display name. Provider-neutral (the backend may be Codex or any
 * OpenAI-compatible API). Imported by both client and server components, so this
 * module must stay free of node-only dependencies. */
export const APP_NAME = "GitLab AI Reviewer";
